package com.dio.aula01;

public class Main {

    public static void main(String[] args) {

/*OPERADORES LÓGICOS
        conjunção: "&&" (and) ->a operação só verdadeira quando ambos operandos são verdadeiros;
        disjunção: "||" (or) ->a operação só é falsa quando ambos operandos são falsos;
        disjunção exclusiva: "^" (xor) ->a operação só verdadeira quando ambos operandos são opostos;
        negação: "!" (inverção) -> operação que inverte o valor lógico de um operando ou expressão;
        */
        boolean b1 = true;
        boolean b2 = false;
        boolean b3 = true;
        boolean b4 = false;

        System.out.println("b1 && b2" + (b1 && b2));
        System.out.println("b1 && b3" + (b1 && b3));

/*CONTROLE DE FLUXO
        decisão: if, if-else, if-else-if, if-else-if-else, switch, operador ternário
        repetição: for, while, do while
        interrupção: break, continue, return
*/



    }
}
